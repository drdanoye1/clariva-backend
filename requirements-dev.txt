# Clariva — backend test/dev dependencies.
# Install alongside requirements.txt:
#   pip install -r requirements.txt -r requirements-dev.txt
#   pytest
#
# httpx (used by FastAPI's TestClient) is already pinned in requirements.txt —
# not duplicated here.

pytest==8.3.3
